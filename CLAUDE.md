# このリポジトリでの作業方針

SIGNATE 練習問題「タイタニックの生存予測」に取り組む学習用リポジトリ。
成果物は `notebooks/` の Jupyter Notebook で、読み手は機械学習を学んでいる本人。

## Notebook は単独で完結させる

作業の進め方の参考として外部のチュートリアルを見ることはあるが、
**Notebook の中にその存在を持ち込まない。**

書かない:

- 「チュートリアルでは〜」「元の記事では〜」といった参照
- 参考にした資料との差分の説明
- 外部資料を読んでいる前提の言い回し

代わりに、**そのコードがそう書かれている理由を、Notebook の中だけで説明する。**

| 避ける書き方 | 置き換え |
| --- | --- |
| チュートリアルに合わせてこの手順を採った | 手順は単純になるが、〜という弱点がある |
| チュートリアルは `[0]` を付けていないので注意 | 付け忘れると〜が起きる |

外部資料の誤りや古さに気づいた場合も、「あちらが間違っている」とは書かず、
**正しいコードを書いた上で、なぜその書き方が必要なのかを説明する。**
半年後に読み返したとき、参照先が消えていても意味が通る状態を保つ。

## ライブラリのバージョン差分を書かない

このプロジェクトは `uv.lock` で固定された環境（pandas 3 系）でしか動かさない。
**過去のバージョンでどう表示されたかは書かない。**

- 「pandas 2 以前は `object` と表示されていた」→ 今の表示だけを説明する
- 「昔は〜という書き方だった」→ 今の書き方だけを説明する

読み手が実際に目にするのは手元の実行結果ひとつだけなので、
別バージョンの話を並べても選択肢が増えて迷うだけになる。

## 解説の書き方

読み手は Python と pandas を学んでいる途中なので、コードの意図をコメントで補う。

- **What より Why。** `.head()` が何をするかだけでなく、なぜここで使うのかを書く
- **出力の読み方を添える。** 実行結果のどこを見ればよいかが分からないと、
  表示されても意味が取れない
- **判断には理由を書く。** 「平均で埋める」なら、中央値ではなく平均を選んだ理由まで書く
- 数値を本文に書くときは**必ず実行して確認する。** 推測で書かない

## Notebook の構成

- ファイル名は `<連番>_<内容>.ipynb`。連番は実行順
- 各 Notebook はカーネルが独立しているので、冒頭で必要なデータを自分で読み込む
- 次の Notebook に渡すデータは `data/` に CSV で保存する（`.gitignore` で除外済み）
- 実行結果は nbstripout により git に保存されない

## データ

- `data/*.csv` は競技規約により再配布できないため git 管理外
- 加工後のデータも同じ扱い
